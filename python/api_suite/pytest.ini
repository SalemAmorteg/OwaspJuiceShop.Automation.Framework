[pytest]
# Directs pytest to look up two levels to find the project root namespace
pythonpath = ../..
testpaths = tests

# Standard execution flags isolating your suite from UI and C# compilation tracks
addopts = --tb=short -v
norecursedirs = .venv .git __pycache__ ui_suite csharp obj bin
